{
  "name": "week27-02",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "cd packages/db && npx prisma generate && cd ../..",
    "build:web": "cd apps/web && pnpm run build",
    "build:be": "cd apps/be && pnpm run build",
    "build:websocket": "cd apps/websocket && pnpm run build",
    "start:web": "cd apps/web && pnpm run start",
    "start:be": "cd apps/be && pnpm run start",
    "start:websocket": "cd apps/websocket && pnpm run start"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "^2.4.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
